<Window xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:Bulochnaya.Windows"
        xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes" x:Class="Bulochnaya.Windows.ReviewsPage"
        mc:Ignorable="d"
        Title="ReviewsPage" Height="450" Width="340.26">
    <Grid>
        <ListView x:Name="Grof" ScrollViewer.HorizontalScrollBarVisibility="Disabled" HorizontalContentAlignment="Left" Margin="0,43,0,0">
            <StackPanel Background="#F19561">
                <Label Content="{Binding Review}" Height="58" Margin="10,0"/>
                <Label  Width="87" HorizontalAlignment="Center" Content="{Binding Name}" Height="29"/>
                <materialDesign:RatingBar Value="{Binding Rating}" Margin="95,0,109,0" Width="114"/>
            </StackPanel>
        </ListView>
        <Button x:Name="review" HorizontalAlignment="Left" Margin="5,4,0,0" VerticalAlignment="Top" Width="109" Height="34" Click="review_Click">
            <Button.Resources>
                <Style TargetType="{x:Type Border}">
                    <Setter Property="CornerRadius" Value="20"/>
                </Style>
            </Button.Resources>
            <Button.Background>
                <ImageBrush ImageSource="/Bulochnaya;component/Images/addreview.png"/>
            </Button.Background>
        </Button>
    </Grid>
</Window>
